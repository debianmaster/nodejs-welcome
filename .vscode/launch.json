{ 
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "localRoot": "${workspaceRoot}",
            "remoteRoot": "/app",
            "name": "Attach to Port",
            "port": 5858,
            "address": "localhost"
        }
    ]
}